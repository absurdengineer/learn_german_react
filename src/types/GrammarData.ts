import type { StandardizedGrammarData } from "../lib/parsers/DataLoader";

export interface GrammarData extends StandardizedGrammarData {
  // Additional properties specific to GrammarData if needed
}
